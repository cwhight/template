<div class="card p-3 m-2 profile-card">

  <h2>Upcoming Jobs</h2>
  <br>
  <% shifts.order(:start_time).each do |shift|  %>
      <% unless shift.completed  %>

        <div class="row justify-content-between m-2">

          <p> <%= shift.job.title %> </p>
          <%= render 'shifts/shift-card-modal', shift: shift %>
        </div>
      <% end %>

  <% end %>
</div>
