<button type="button" class="btn btn-ghost" data-toggle="modal" data-target="#user<%=shift.id%>">
  <%= "View Details" %>
</button>

<div class="modal fade" id="user<%=shift.id%>" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLongTitle"><%= "#{shift.job.title} on #{Time.parse(shift.start_time)}" %></h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <div class="card p-3">

            <div class="row p-2 justify-content-between">
                <div>
                  <p><%= Time.parse(shift&.start_time)&.strftime("%k:%M") %> to <%= Time.parse(shift&.end_time)&.strftime("%k:%M") %></p>
                </div>
                <div>
                  <p>Hourly Rate: <%= humanized_money_with_symbol(shift.price) %></p>
                  <% if shift.end_time < DateTime.now && shift.user == current_user %>
                    <%= render 'shared/review', shift: shift, review_target: shift.job.user %>
                  <% end %>

                </div>

          </div>
        </div>

      </div>
    </div>
  </div>
</div>

