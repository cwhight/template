var favToggle = document.querySelector("#favourite-toggle<%=@job.id%>")

function replaceFav(innerHTML) {
  favToggle.innerHTML = innerHTML
}

replaceFav('<%= link_to '', job_favourite_path(@job, @favourite), method: :delete, class: "favourite-btn fas fa-heart", remote: true  %>')

console.log("hello")
